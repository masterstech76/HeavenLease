/* ============================================================
 * HeavenLease — heavenlease page module
 * Extracted from heavenlease.html (1 inline block).
 * Removes all inline <script> from the page per project rule.
 * ============================================================ */

window.location.replace('index');
