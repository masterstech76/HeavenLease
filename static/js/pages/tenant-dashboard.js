/* ============================================================
 * HeavenLease — tenant-dashboard page module
 * Extracted from tenant-dashboard.html (1 inline block).
 * Removes all inline <script> from the page per project rule.
 * ============================================================ */

window.location.replace('dashboard');
