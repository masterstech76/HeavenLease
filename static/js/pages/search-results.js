/* ============================================================
 * HeavenLease — search-results page module
 * Extracted from search-results.html (1 inline block).
 * Removes all inline <script> from the page per project rule.
 * ============================================================ */

window.location.replace('properties');
